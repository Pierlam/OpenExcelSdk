# What is OpenExcelSdk ?

OpenExcelSdk is a backend dotnet library to use Excel (xlsx) very easily.

It's an open-source .NET library over written in C#/NET8.

The only dependency is DocumentFormat.OpenXml (Open XML SDK), the official Microsoft library to work with Excel files.

